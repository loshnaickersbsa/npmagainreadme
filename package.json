{
  "name": "nodeprojectsimple",
  "version": "1.0.0",
  "description": "simple lite server with npm install",
  "main": "index.html",
  "scripts": {
    "start": "npm run lite",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lite" : "lite-server"
  },
  "author": "losh",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.6.1"
  }
}
